/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.linenclub.com", "example.com", "plus.unsplash.com", "images.unsplash.com"], // Add allowed domains here
  },
};

module.exports = nextConfig;
